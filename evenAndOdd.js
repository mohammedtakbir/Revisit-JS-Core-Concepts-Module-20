const evenAndOdd = (num) => {
    if (num % 2 === 0) {
        return true;
    } else {
        return false;
    }
}

const result = evenAndOdd(130);
// console.log(result);

//* check whether a number is odd or not
const isOdd = (num) => {
    if (num > 0) {
        return true;
    } else {
        return false;
    }
}
const res = isOdd(10);
console.log(minutes)